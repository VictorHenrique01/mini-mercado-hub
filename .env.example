# URL do backend Flask em produção
VITE_API_URL=https://estoque-web-3513.onrender.com

# NOTA: As credenciais do Twilio (TWILIO_SID, TWILIO_TOKEN, TWILIO_PHONE) 
# e a SENHA_JWT ficam configuradas no backend Flask, não no frontend.
# O frontend apenas consome a API REST.
